// Intentionally empty — see `DashboardTab 2.swift` for the canonical declaration.
// This placeholder exists because the Xcode project references `DashboardTab 2.swift`,
// but Swift Package Manager picks up every .swift file in the directory. Keeping this
// as a stub prevents duplicate enum declarations across both build systems.
import Foundation
